def find_max(numbers):
    # 리스트에서 최댓값 찾기
    max_value = max(numbers)
    return max_value

# 테스트를 위한 리스트
numbers = [3, 7, 2, 9, 1, 5]

# 최댓값 찾기 함수 호출
max_number = find_max(numbers)
print("최댓값:", max_number)
